# GHQ

GitHub, on the command line. Super hacky and experimental. Largely vibe coded as a proof-of-concept.

## TODO

- Decouple the command bar from the "jump to line number" feature. The `:N` go-to-line behavior is currently wired through the command/picker result path; it should be its own first-class action. `g` could be a good dedicated shortcut for this.
